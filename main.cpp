#include <glad/glad.h>
#include <GLFW/glfw3.h>
#include <iostream>
#include <fstream>
#include <sstream>

int main() {
    bool running = true;
    
    while(running) {
        Console.clear()
    }
}